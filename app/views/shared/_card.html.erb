    <div class="card">
      <div class="img-frame" >
        <!-- The Modal -->
        <%= render partial: "shared/modal_show", locals: { item: item } %>
      </div>
      <div class='icon-tags'>
        <%= link_to "", root_path, class: "fa fa-heart-o" %>
        <%= link_to "", edit_item_path(item), class: "fa fa-pencil" %>
        <%= link_to "", item_path(item), method: :delete,  data: { confirm: " Are you sure you want to delete this item?" }, class: "fa fa-times" %>
      </div>

      <div class="card-store"><%= item.original_store %></div>
      <div class="card-item-name">
        <%= item.name %>
      </div>
      <div class="card-item-price">
        <p><%= item.price %>€</p>
      </div>

      <!-- <p>Your price goal: <%= item.user_price %></p> -->

      <div>
        <%= link_to "BUY", item.url, target: "_blank", class:"btn btn-primary" %>
      </div>

    </div>
